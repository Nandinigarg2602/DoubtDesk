import './Overlays.css';

/**
 * Overlays — Minimal vignette only.
 * Grain, scanlines, and custom cursor removed per Podium-inspired redesign.
 */
export default function Overlays() {
  return (
    <div className="overlay-vignette" aria-hidden="true" />
  );
}
